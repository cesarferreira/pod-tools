#!/usr/bin/env ruby
require 'bundler/setup'
require 'colorize'
require 'pod-cleaner'

current_path = `pwd`
current_path = current_path.tr("\n","")

PodCleaner.init(current_path)
PodCleaner.reinstall(current_path)
